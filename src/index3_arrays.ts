let numbers: number[] = [1, 2, 3]; // this is the type anotation for the array of numbers or we can write
// let numbers = [1,2,3,4] like this
// numbers.forEach(element => numbers. ); after the dot we can access the functions

let fruits: string[] = ["apple", "mango", "kiwi"];
